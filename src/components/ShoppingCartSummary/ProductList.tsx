import { CartItemType } from '../../AvocadoTypes/types';
import React from 'react';

const ProductList = ({ items }: { items: any }) => {
  return <div className="flex flex-col">items</div>;
};

export default ProductList;
